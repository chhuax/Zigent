# AGENTS.md — 入场须知（AI Agent 与新加入者必读）

> **你是被派来参与 Zigent 的 AI Agent。动手前先把本文件读完（约 5 分钟）。**
> 本文件的目标：让你**在没有任何对话历史的情况下**也能理解项目背景、知道边界、知道从哪开始。

---

## 0. 30 秒背景

**Zigent** 是一个**智能编码 Agent**（对标 Claude Code）：能读写代码、执行命令、跨轮次保持上下文，并把过程实时吐给前端。

**本项目用 Zig 0.16 从零实现一个编码 Agent 内核**，**只做内核**：平台外围约 28 万行明确排除（前端 12.2 万、kanban 平台 7.1 万、企业认证 1.2 万、编译期反射元数据 9.2 万）。设计前做过一轮逐文件工程盘点（26.8 万行主代码 + 31.5 万行测试量级）。

**为什么要做（三条正当理由，不是"Zig 更快"）**：
1. **消灭一整类事故** —— 靠反射做序列化 + 编译期元数据漏登记，会让多态子类在原生二进制下静默序列化成 `{}` → provider 报 2013。元数据清单已积累到 **92,552 行 JSON / 11,428 个类条目**。Zig 无反射 → 这类 bug 在语言层面消失。
2. **交叉编译** —— `zig build -Dtarget=…` 一条命令出全平台产物，不需要每个平台配一台构建机。
3. **体积与启动** —— 目标 <10ms 启动、5–15MB 二进制。

**当前阶段**：设计已完成（18 份文档 / 79 张图），有一个 spike 骨架（63 个契约测试）。**下一步是装 Zig 跑 spike，然后按契约冻结 → 五流并行 → 内核集成推进。**

---

## 1. 仓库

| 项 | 内容 |
|---|---|
| **仓库根目录** | `/data/workspace/zigent` |
| 你该做什么 | ✅ 写代码、写文档、随便改 |

> **本仓库自包含**：设计文档、契约、工作包、spike 骨架都在这里，**不依赖任何其它仓库**。

---

## 2. ⚠️ 路径约定（最容易踩的坑）

**本项目文档体系里所有相对路径，都相对本仓库根目录。**

| 你看到的写法 | 实际位置 |
|---|---|
| `docs/analysis/…` | `docs/analysis/…`（本仓库） |
| `spike/zig-m0/src/sse.zig` | `spike/zig-m0/src/sse.zig`（本仓库） |
| `docs/web/` | `docs/web/`（本仓库，文档的 HTML 版） |

**判别方法**：路径以 `spike/` / `docs/` / `src/` 开头 → 本仓库内文件。

---

## 3. 已定决策（**不要再讨论、不要试图推翻**）

| # | 决策 |
|:--:|---|
| 1 | **对标 Claude Code**；交付物先是内核 + 本地 Web 服务 |
| 2 | **不复用现有 React 前端**；将来用 **Tauri + Rust** 新做（简洁 Web UI，**不是看板**） |
| 3 | **Web-first**；桌面壳随后加（壳本体 1–2 周，生产化 2–4 人月） |
| 4 | **TUI 不做**，外包给另一个团队（它只依赖 `06-对外协议.md`） |
| 5 | **不接入企业平台**（企业 IM/CAS/托管 provider 全部不做）→ 认证退化为纯 BYOK |
| 6 | **Windows / WSL 延后**，但保留抽象（watcher 可插拔、OS 分支留形状） |
| 7 | **记忆归档用方案 A**（JSONL + grep + 打分），**不引入外部数据库或归档格式** |
| 8 | **kanban 平台与看板形态全部排除** |
| 9 | **首期只做**：8 个工具 / 压缩三层 / 恢复三态 |
| 10 | 类型命名以 `04-核心数据模型设计.md` §13 为**唯一权威**（`perm.Verdict` 而非 `Decision`） |

---

## 4. 禁区（违反会直接造成损失）

1. 🛑 **不要相信旧架构文档的转述** —— 已实测 8 处「文档说法 vs 实际行为」不一致（`AUTOCOMPACT_BUFFER_TOKENS` 文档说 13,000 实际 40,000；`PayloadStore` 类根本不存在；`FilesystemMutual`… 详见 `spike/zig-m0/移植雷区.md` §L）。
 **纪律：契约只从代码读，不从文档读。**
2. 🛑 **不要在搜索/替换时用全仓范围** —— 教训：一次「后缀替换」把别人文档里的长文件名尾部也改了（`…-migration-design.md` → `…-13-持久化格式设计.md`），误伤 7 个文件。**只改你自己的文件，用精确路径。**
3. 🛑 **不要引入 GPL/AGPL 依赖** —— 只用 Apache/MIT/BSD/EPL 商用友好许可（含 Zig 生态与 C 库）。
4. 🛑 **不要改 `04-核心数据模型设计.md` / `06-对外协议.md` 的字段名** —— 它们是**对外契约**，改了会让 TUI/Web 团队与transcript 全部失配。要改走变更请求。
5. 🛑 **不要改完 `docs/web/` 下的图就宣布完成** —— 手绘 SVG 的缺陷几乎都是**静默失败**：不报错，只是画错。改完必须 `bash docs/web/shoot-figures.sh` 逐张**真的看一眼**。已实测踩中三类：
 - 不存在的 CSS 变量 → 该属性被整个丢掉，`fill` 掉成 SVG 默认**黑色**（曾把 `.ask` 方块画成黑块，被当成「深色主题配色」放过一整个回合）；
 - SVG 里写了 `<b>`/`<span>`/`<p>` 等 HTML 标签 → 解析器**提前退出 `<svg>` 命名空间**，整张图**塌成一行散字**（`module-deps` 中招；顺带查出早先的图 4、图 5 也一直是塌的，没人逐张看过）；
 - 标签垫了底衬却仍被连线穿过 → SVG **没有 z-index，绘制顺序即层级**，底衬与文字必须放在 `</svg>` 之前。
 前两类 `build.py` 已加守卫、构建期 `exit 1`；第三类只能靠眼睛。详见 `docs/web/README.md`。

---

## 5. 技术栈与版本纪律

| 项 | 约定 |
|---|---|
| 语言 | **Zig**（pre-1.0，每版都有破坏性变更） |
| 版本 | `build.zig.zon` 里锁 `minimum_zig_version`；**只在偶数版本升级**；CI 固定 toolchain |
| **开发基线** | **锁定 `0.16.0`**（已实测的最新稳定版）。**0.17 仍是 dev**（`0.17.0-dev.2163`），作为跟踪目标 —— 里程碑时评估升级，不跟 master |
| 为什么不用 0.17 | ① 未发布，跟 master 会被持续打断；② 项目最大的适配面 `std.Io` **在 0.16 就已定型**（实测两版 `std.Io` 成员表完全相同），用 0.17 换不到便宜；③ 0.17 的增量主要在 **build/包管理 API**（`PackageEntry` / `PackageOptions` / `ConfigureDependency` 新增，`PkgConfig*` 移除），等有真实构建脚本后再评估成本更准 |
| 已实测 | `std.Io.Mutex` + `ArrayListUnmanaged = .empty` + `DebugAllocator` 的同一份代码在 0.16.0 与 0.17.0-dev **都能编译**；`build.zig` 的 `root_module` 写法两版通用 |
| ⚠️ 0.16 重大变化 | `std.Thread.Mutex` **已删除** → `std.Io.Mutex`，且 `lock(io)` / `wait(io, m)` 都要 **`io` 实例**；`std.crypto.random`、`std.posix.getenv`、`std.Thread.sleep`、`std.fs.File` 全部搬进 `std.Io`；`ArrayListUnmanaged(T) = .empty`（**不是 `= .{}`**）；`Allocator.VTable` 新增 `remap` 字段。详见 `11-stdIo贯穿方案.md` |
| 依赖 | 起步用 **libcurl**（HTTP/TLS，MIT/X）而非 `std.http.Client`；SQLite 仅当将来要 BM25 时引入 |
| 交付物 | 单二进制，静态链接；桌面壳按 sidecar 拉起 |

---

## 6. 阅读顺序（按你的角色）

**所有人**：`README.md` → `docs/analysis/2026-09-19-00-入口文档.md`（它有按角色的细路径）

| 你要做的事 | 必读 |
|---|---|
| 写内核代码 | `04-核心数据模型设计` → `spike/zig-m0/移植雷区` → `03-架构详解` → 对应模块文档 |
| 写契约/协议 | `04` + `06-对外协议` |
| 写工具 | `05-工具契约设计` |
| 写权限 | `07-权限与安全设计` |
| 写测试/验收 | `12-测试与验收设计` |
| 理清"要做什么" | **`18-工作包清单`**（可执行的任务分解） |
| 决策/评估 | `02-功能清单与核心优先` + `17-主设计方案` §0/§7 |

---

## 7. 你应该从哪开始（按当前状态）

```
第 0 步（必须先做）：装 Zig 0.16.0 → cd spike/zig-m0 && zig build test
 期望：63 个契约测试。当前有 5 处 0.16 适配未完成（见 spike/zig-m0/README.md §1.1）
第 1 步：采基线（启动时间 / 峰值 RSS / 体积 / benchmark 分数）
 ⚠️ 必须在 M0 收尾前采 —— 否则实现后无法对比第 2 步：冻结契约（04 + 06）→ 这是并行开发的唯一前提第 3 步：按 18-工作包清单领活（WP-01 ~ WP-22）
```

---

## 8. 术语约定（写代码/文档时必须一致）

| 中文 | 英文/代码 | 说明 |
|---|---|---|
| 一轮（模型请求 + 其工具结果） | `Turn` | **配对不变量的类型化载体**，不是"回合数" |
| 一次 submitMessage 全程 | run | 由多个 Turn 串成 |
| 工具调用请求 / 结果 | `tool_use` / `tool_result` | 必须**紧邻配对** |
| 事件 | `StreamEvent` | 33 种，核心 19 + 扩展 14 |
| 契约层 | `common/` | **零内部依赖**，所有模块依赖它 |
| 运行时上下文 | `Rt` | 携带 `gpa` + `io` + `settings`，**唯一注入点，禁止全局单例** |
| 权限结论 / 完整结果 | `Verdict` / `Outcome` | **不要用 `Decision`**（历史上 3 个枚举重名，见 `04` §13） |

完整术语表见 `00-入口文档.md` §7。

### 8.1 叙事视角（**公开仓库，写文档前必读**）

本仓库是**一个全新项目**。所有文档必须站在**新项目的第一人称**写，只描述本设计本身。

| ❌ 不要写 | ✅ 要写 |
|---|---|
| 重写 / 重写主方案 / 全量重写 | 实现 / 主设计方案 |
| 砍掉 / 砍 / 砍完 | 不做 / 去掉 / 不纳入 |
| 旧实现 / 现有实现 / 前身 | （直接陈述本设计即可） |
| 为什么要重写 | 为什么要做 / 立项动机 |

**保留正当用法**：「重写 `http_server.zig`」「只解析、不重写文件」「超长文本直接砍」「截断/重写」
—— 这些指**代码级动作**或**存量数据的兼容处理**，与项目定位无关，不要改。

> 判别方法：把句子里的「重写」换成「实现」，如果意思变了 → 是代码级用法，保留；
> 如果只是把「做」说成「重写」→ 是定位措辞，要改。
>
> 工程口径的出处见 `docs/internal/`（不进 git，只在本机可用）。

---

## 9. 交付纪律

1. **文档与代码同步**：改了契约 → 同步改 `04`/`06` 并记入变更记录。
2. **测试优先于实现**（三个例外，它们在设计阶段就必须先写测试）：配对不变量、数据兼容（transcript round-trip + `enc:1:`）、桌面壳交付契约。
3. **每个工作包都有可执行验收**（见 `18-工作包清单`）——不接受"看起来能跑"。
4. **两条客观判据**：① 同一份 golden fixture **同时通过双后端**；② benchmark 分数**不低于基线**。
5. **不确定就查代码，不要猜**；查不到就在工作包文档里记「待确认」，不要编。
